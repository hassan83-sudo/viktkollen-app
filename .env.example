# Supabase client auth, cloud backup and sync.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional server-side Supabase helpers for verification/API flows.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
ACCOUNT_DELETION_ENABLE_AUTH_DELETE=false

# Server-only OpenAI key. Do not prefix with VITE_.
# Required only when remote AI/photo provider should be enabled in production.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
OPENAI_COACH_MODEL=gpt-4.1-mini
VOICE_AI_MODEL=gpt-4o-mini-realtime-preview
VOICE_IDLE_TIMEOUT_MS=45000
VOICE_MAX_SESSION_MS=180000
OPENAI_COACH_TIMEOUT_MS=15000
OPENAI_COACH_RATE_LIMIT_MAX=8
OPENAI_LEGACY_AI_RATE_LIMIT_MAX=20
OPENAI_MAX_OUTPUT_TOKENS=900
NUTRITION_PHOTO_MODEL=gpt-4.1-mini
NUTRITION_PHOTO_MAX_FILE_BYTES=8388608
NUTRITION_PHOTO_TIMEOUT_MS=15000
NUTRITION_PHOTO_RATE_LIMIT_WINDOW_MS=600000
NUTRITION_PHOTO_RATE_LIMIT_MAX=12
MEAL_ANALYSIS_RATE_LIMIT_MAX=10
BODY_ANALYSIS_RATE_LIMIT_MAX=4

# Keep false unless OPENAI_API_KEY is configured and route verification passes.
VITE_NUTRITION_PHOTO_REMOTE_ENABLED=false
VITE_NUTRITION_PHOTO_MAX_FILE_MB=8
VITE_NUTRITION_PHOTO_TIMEOUT_MS=15000
VITE_NUTRITION_PHOTO_RATE_LIMIT_MAX=12

# AI-örat (Smart kamera). SERVER-ONLY - never prefix with VITE_. Placeholders only, no secrets here.
# The hop /api/ai-ear/interpret stays "not configured" (HTTP 503) unless AI_EAR_ENABLED=true.
AI_EAR_ENABLED=false
# Private Cloud Run service URL (also used as the Google ID-token audience unless AI_EAR_BACKEND_AUDIENCE is set).
AI_EAR_BACKEND_URL=
AI_EAR_BACKEND_AUDIENCE=
# Service-account JSON (plain or base64) of an account with roles/run.invoker on the Cloud Run service. Set only in Vercel server env.
AI_EAR_GCP_SERVICE_ACCOUNT_JSON=
AI_EAR_MAX_BYTES=4194304
AI_EAR_TIMEOUT_MS=28000
AI_EAR_RATE_LIMIT_MAX=10
